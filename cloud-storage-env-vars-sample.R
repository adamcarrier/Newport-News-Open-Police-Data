## Put your Google Cloud Storage API variables here
GCS_CLIENT_ID = "mykey"
GCS_CLIENT_SECRET = "mysecretkey"
GCS_DEFAULT_BUCKET = "my-default-bucket"

# Needs to be a Service Account Key (see https://console.cloud.google.com/apis/credentials)
GCS_AUTH_JSON_FILE = "service-auth.json"

# See here for API keys: https://console.cloud.google.com/apis/credentials
GC_API_KEY = "myapikey"
